-- Lua standard library extensions
return {
    tablex = require(script.tablex),
    stringx = require(script.stringx),
    mathx = require(script.mathx),
    functional = require(script.functional),
    sort = require(script.sort),
    async = require(script.async),
    timer = require(script.timer),
    formal = require(script.formal),
    folder = require(script.folder),
}